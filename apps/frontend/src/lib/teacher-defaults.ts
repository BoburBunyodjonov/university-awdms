/**
 * New teachers are created without a scientific degree; `degreeName` is still
 * required by the API. This placeholder is replaced when a PhD is registered
 * on the Scientific degrees admin page.
 */
export const TEACHER_DEGREE_PLACEHOLDER = '—';
